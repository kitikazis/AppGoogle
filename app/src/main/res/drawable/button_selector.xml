<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado presionado -->
    <item android:state_pressed="true" android:drawable="@drawable/button_background" />
    <!-- Estado seleccionado (cuando el botón está "activado") -->
    <item android:state_selected="true" android:drawable="@drawable/button_background" />
    <!-- Estado normal -->
    <item android:drawable="@color/white" />
</selector>
